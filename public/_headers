# Netlify headers configuration for rad.io
# Required for SharedArrayBuffer support (ADR-0025)

/*
  # Cross-Origin Isolation (required for SharedArrayBuffer)
  # See: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/SharedArrayBuffer#security_requirements
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
  
  # Security headers
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  X-XSS-Protection: 1; mode=block
  Referrer-Policy: strict-origin-when-cross-origin
  
  # Permissions Policy (restrict powerful features)
  Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=()
  
  # Content Security Policy (adjust as needed for your resources)
  # Note: 'unsafe-inline' and 'unsafe-eval' may be needed for dev builds
  # Content-Security-Policy: default-src 'self'; script-src 'self' 'wasm-unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'self' https:; worker-src 'self' blob:; font-src 'self' data:

# WASM files need explicit CORP header
/*.wasm
  Cross-Origin-Resource-Policy: same-origin
  Content-Type: application/wasm

# JavaScript files
/*.js
  Cross-Origin-Resource-Policy: same-origin

# Source maps
/*.map
  Cross-Origin-Resource-Policy: same-origin
