// Deprecated: StatusFooter has been removed. This placeholder test file remains
// temporarily to avoid churn in test discovery until CI is updated. It contains
// a single no-op test to signal the deprecation without failing the suite.

describe("StatusFooter (deprecated placeholder)", () => {
  it("is deprecated and replaced by StatusBar", () => {
    expect(true).toBe(true);
  });
});
